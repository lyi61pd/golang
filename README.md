# Golang 学习项目

这是一个系统化的 Golang 学习项目，包含了从基础语法到进阶特性的完整教程和代码示例。

## 项目结构

### 📁 syntax/ - Go 语言基础语法教程

包含 Go 语言核心语法的学习示例，按照从易到难的顺序组织：

| 文件夹 | 说明 | 对应教程步骤 |
|--------|------|--------------|
| `hello/` | Hello World 程序，Go 程序入口 | 第三步 |
| `types/` | 基本数据类型（int, float, string, bool） | 第四步 |
| `vars/` | 变量声明与常量定义 | 第五步 |
| `control/` | 流程控制（if, for, switch） | 第六步 |
| `mathutil/` | 自定义包示例（数学工具包） | 第七步 |
| `usemath/` | 包的导入与调用示例 | 第七步 |
| `funcs/` | 函数定义与调用 | 第八步 |
| `collections/` | 数组、切片（slice）、map 的用法 | 第九步 |
| `structs/` | 结构体（struct）和方法（method） | 第九步 |
| `interfaces/` | 接口（interface）及其实现 | 第十步 |
| `concurrency/` | 并发模型（goroutine、channel） | 第十一步 |
| `testing/` | 单元测试与表驱动测试 | 第十三步 |
| `reflection/` | 反射机制（reflection） | 第十五步 |
| `generic/` | 泛型（generics，Go 1.18+） | 第十六步 |
| `sync/` | 协程同步（WaitGroup） | 第十七步 |
| `pointer/` | 指针的使用 | 进阶内容 |
| `anonymous/` | 匿名函数与闭包 | 进阶内容 |
| `deferpanic/` | defer、panic、recover 错误处理 | 进阶内容 |
| `error/` | 错误处理最佳实践 | 进阶内容 |
| `typeassert/` | 类型断言与类型转换 | 进阶内容 |

### 📁 packages/ - 第三方包与实战项目

包含常用第三方包的学习示例和实战项目：

| 文件夹 | 说明 | 技术栈 |
|--------|------|--------|
| `go-context/` | Context 上下文详解（取消、超时、传值） | 标准库 context |
| `gin-demo/` | Gin Web 框架实战项目，包含完整的项目结构 | Gin + GORM + SQLite |
| `gorm-demo/` | GORM ORM 框架学习示例 | GORM |
| `gin-swagger/` | Gin + Swagger API 文档示例 | Gin + Swagger |
| `expr-lang/` | Expr 表达式语言学习（动态表达式求值） | expr-lang |
| `ali-kms/` | 阿里云 KMS 密钥管理服务 SDK 使用示例 | 阿里云 SDK |

### gin-demo 项目结构

gin-demo 是一个标准的 Go Web 项目示例，采用分层架构：

```
gin-demo/
├── cmd/server/          # 程序入口
├── internal/            # 内部代码（不对外暴露）
│   ├── handler/        # HTTP 处理器（控制器）
│   ├── middleware/     # 中间件（日志、认证等）
│   ├── model/          # 数据模型
│   ├── repository/     # 数据访问层
│   └── service/        # 业务逻辑层
├── go.mod              # Go 模块依赖管理
└── main.go             # 主程序入口
```

## 学习路径

### 🎯 初级阶段（基础语法）

1. **环境搭建**：安装 Go 并配置环境变量
2. **Hello World**：`syntax/hello/`
3. **基础语法**：`syntax/types/`、`syntax/vars/`、`syntax/control/`
4. **函数与数据结构**：`syntax/funcs/`、`syntax/collections/`
5. **面向对象**：`syntax/structs/`、`syntax/interfaces/`

### 🚀 中级阶段（并发与进阶特性）

6. **并发编程**：`syntax/concurrency/`、`syntax/sync/`
7. **Context 上下文控制**：`packages/go-context/`
8. **包管理**：`syntax/mathutil/`、`syntax/usemath/`
9. **测试**：`syntax/testing/`
10. **进阶特性**：`syntax/generic/`、`syntax/reflection/`

### 💪 高级阶段（实战项目）

11. **错误处理**：`syntax/error/`、`syntax/deferpanic/`
12. **Web 开发**：`packages/gin-demo/`
13. **数据库**：`packages/gorm-demo/`
14. **第三方集成**：`packages/ali-kms/`、`packages/expr-lang/`

## 快速开始

### 运行基础语法示例

```bash
# 进入 syntax 目录
cd syntax

# 运行 Hello World
cd hello
go run hello.go

# 运行其他示例（以类型为例）
cd ../types
go run types.go
```

### 运行 Context 示例

```bash
# 进入 go-context 目录
cd packages/go-context

# 运行 Context 学习示例
go run main.go
```

### 运行 Gin Web 项目

```bash
# 进入 gin-demo 目录
cd packages/gin-demo

# 安装依赖
go mod tidy

# 运行项目
go run main.go
```

### 运行单元测试

```bash
# 进入测试目录
cd syntax/testing

# 运行所有测试
go test

# 运行测试并显示详细信息
go test -v
```

## 学习资源

### 官方文档
- [Go 官方文档](https://golang.org/doc/)
- [Go 语言标准库](https://golang.org/pkg/)
- [Effective Go](https://golang.org/doc/effective_go.html)

### 推荐教程
- [Go by Example](https://gobyexample.com/)
- [Go 语言圣经（中文版）](https://books.studygolang.com/gopl-zh/)
- [Go 语言高级编程](https://chai2010.cn/advanced-go-programming-book/)

### 常用框架
- [Gin Web Framework](https://gin-gonic.com/)
- [GORM ORM](https://gorm.io/)
- [Cobra CLI](https://github.com/spf13/cobra)

## 注意事项

1. 确保已安装 Go 1.18 或更高版本（支持泛型特性）
2. 建议按照学习路径的顺序逐步学习
3. 每个示例都有详细的代码注释，建议仔细阅读
4. 动手实践是学习编程的最佳方式，建议完成每个练习

## 贡献

欢迎提交 Issue 或 Pull Request 来改进这个学习项目！

---

